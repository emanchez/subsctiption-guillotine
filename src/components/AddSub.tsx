"use client";
import { SubCardProps } from "@/components/SubsCard";

const AddSub = (props: SubCardProps) => {
  return (
    <div>
      <form>some form with api sub as the shape</form>
    </div>
  );
};

export default AddSub;
